(function () {
    'use strict';

    const successModal = document.getElementById('categories-modal');

    successModal.addEventListener('modalopen', function(event) {
        const data = event.detail || {};
    });
}());